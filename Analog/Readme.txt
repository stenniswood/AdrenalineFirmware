Note:  

Always Do a "make clean" prior to make!

Each project defines the pins differently, this affects some of the core modules.
So a core *.o file may not work on Analog board compared to LCD for instance.

A make clean will force a recompile with the specific app's header.

